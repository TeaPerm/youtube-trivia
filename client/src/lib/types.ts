export type FormData = {
  username: string;
};

export interface VideoResponse {
    title: string;
    videoId: string;
    averageColor: string,
    videoDuration: string,
    viewCount: string;
    thumbnail: string;
    videoUrl: string;
}